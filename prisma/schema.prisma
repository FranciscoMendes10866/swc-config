generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Base {
  id      Int    @id @default(autoincrement())
  message String
}
